/** Authored icons: 16px grid, 1.5px round stroke in currentColor, always decorative (the
 * control carries the accessible name). */
export function CloseIcon() {
  return (
    <svg className="tw-icon" viewBox="0 0 16 16" width="16" height="16" aria-hidden="true" focusable="false">
      <path d="M4 4l8 8M12 4l-8 8" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" />
    </svg>
  );
}
